#!/bin/zsh

# File: mac_scripts/gt
# Git tool with the following commands:
#   gt push              - Force push (safe with --force-with-lease)
#   gt fetch [branch]  - Fetch specified branch, or both main and develop if no argument
#   gt clean [--force] - Clean up gone branches (use --force to force delete)

# Get script directory
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]:-$0}")" && pwd)"

# Source and run with arguments
source "$SCRIPT_DIR/functions/git-tools"
gt "$@"